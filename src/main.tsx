import React from 'react'
import ReactDOM from 'react-dom/client'
import AdaptiveLearningSystem from './adaptive_learning_final'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AdaptiveLearningSystem />
  </React.StrictMode>,
)